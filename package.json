{
  "name": "nepal-federal-parliament-directory",
  "version": "1.2.0",
  "description": "Contact directory for Nepal's Federal Parliament (House of Representatives + National Assembly), leadership offices, and committees.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "npx serve public -l 3000",
    "start": "npx serve public -l 3000",
    "validate-json": "node -e \"['members','leadership','committees','statistics','metadata','search-index'].forEach(n=>{const f='public/assets/data/'+n+'.json';JSON.parse(require('fs').readFileSync(f,'utf8'));console.log('OK',f)})\"",
    "validate-js": "for f in public/assets/js/*.js public/sw.js; do node --check \"$f\" || exit 1; done",
    "validate": "npm run validate-json && npm run validate-js"
  },
  "keywords": ["nepal", "parliament", "civic-tech", "open-data", "directory"],
  "engines": {
    "node": ">=18"
  }
}
